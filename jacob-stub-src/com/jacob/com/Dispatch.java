package com.jacob.com;

/** Stub for JACOB Dispatch — compile-time only, real JACOB JAR used at runtime. */
public class Dispatch {
    public static Variant get(Dispatch dispatch, String property) { return null; }
    public static Variant call(Dispatch dispatch, String method, Object... args) { return null; }
}
