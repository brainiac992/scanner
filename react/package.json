{
  "name": "scanner-bridge-react",
  "version": "1.0.0",
  "description": "React components for the Scanner Bridge webapp integration",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "notes": "These components are meant to be integrated into an existing React project. Copy hooks/ and components/ into your src/ directory and import ScannerPanel where needed."
}
